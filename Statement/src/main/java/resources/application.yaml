server:
  port: 8080

spring:
  application:
    name: statement
logging:
  level:
    root: INFO
    com.creditbank.statement: DEBUG

deal:
  url: ${DEAL_URL}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html