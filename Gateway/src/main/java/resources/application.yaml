server:
  port: 8080

spring:
  application:
    name: gateway
logging:
  level:
    root: INFO
    com.creditbank.gateway: DEBUG

deal:
  url: ${DEAL_URL}
statement:
  url: ${STATEMENT_URL}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html